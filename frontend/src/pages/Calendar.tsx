import type { FC } from "react";
import { ComingSoon } from "../components/ui/ComingSoon";

export const Calendar: FC = () => (
  <ComingSoon
    title="Calendar"
    summary="Results announcements, AGMs, ex-dividend and book-closure dates in a month grid, with estimated dates clearly labelled."
    phase="phase 7"
  />
);
