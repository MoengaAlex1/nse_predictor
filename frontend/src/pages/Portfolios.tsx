import type { FC } from "react";
import { ComingSoon } from "../components/ui/ComingSoon";

export const Portfolios: FC = () => (
  <ComingSoon
    title="Portfolios"
    summary="Track lots, cost basis, unrealised profit and loss in KES, sector weights, dividend income and time-weighted return against the NSE 20."
    phase="phase 5"
  />
);
